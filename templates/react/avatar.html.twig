{% extends 'base.html.twig' %}

{% block title %}Cevenne's Gate
{% endblock %}

{% block body %}
	<div class="container">
		<div class="row">
			<div class="col-12 text-center">
				<p>Bienvenue aux portes de Cevenne,
					{{app.user.username}}</p>
				<p>Choisissez votre avatar pour
					{{app.user.character.name}}</p>
				<div {{ react_component('Heroe') }}></div>
			</div>
		</div>
	</div>
{% endblock %}
